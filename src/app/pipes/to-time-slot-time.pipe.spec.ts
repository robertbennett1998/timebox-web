import { ToTimeSlotTimePipe } from './to-time-slot-time.pipe';

describe('ToTimeSlotTimePipe', () => {
  it('create an instance', () => {
    const pipe = new ToTimeSlotTimePipe();
    expect(pipe).toBeTruthy();
  });
});
